pre-commit:
  piped: true
  commands:
    format:
      run: pnpm exec biome check --write --staged --no-errors-on-unmatched
      stage_fixed: true
    lint:
      run: pnpm lint
    check-types:
      run: pnpm check-types
    test:
      run: pnpm test
    build:
      run: pnpm build
